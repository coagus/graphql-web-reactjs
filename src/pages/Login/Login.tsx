import React from 'react'
import styled from 'styled-components'

const Login: React.FC = () => {
  return <LoginStyle>Login</LoginStyle>
}

export const LoginStyle = styled.h1``

export default Login
